# Global Variable
# Convention across the repo (see MapleStoryAutoLevelUp.py L528 / L648 / L919,
# plus cv2.VideoWriter which requires (width, height) order):
#   (WIDTH, HEIGHT) = (horizontal_pixels, vertical_pixels)
WINDOW_WORKING_SIZE = (1296, 700)  # (WIDTH, HEIGHT)  horizontal,vertical
